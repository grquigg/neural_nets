#ifndef LIN_ALG_H
#define LIN_ALG_H

#include <vector>

void dotProduct(std::vector<std::vector<float>>& inputs, std::vector<std::vector<float>>& weights, std::vector<std::vector<float>> * product);

#endif